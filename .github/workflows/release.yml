name: Publish to PyPI

on:
  workflow_dispatch:
    inputs:
      semver:
        description: The semver to use
        required: true
        default: patch
        type: choice
        options:
          - patch
          - minor
          - major
  push:
    branches:
      - master
    tags:
      - 'v*'
  # pull_request:
  #   types: [closed]

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install build twine bump-my-version

      - name: Optic Release Automation with custom bump
        uses: nearform-actions/optic-release-automation-action@feat/support-custom-bump-command
        # uses: hakimnajmipocketwalla/optic-release-automation-action@feat/custom-version-bump
        with:
          github-token: ${{ secrets.GH_PAT }}
          bump-command: bump-my-version bump
          semver: ${{ github.event.inputs.semver }}
          build-command: |
            rm -rf dist/
            python -m build
          commit-message: "Release {version}"

      - name: Publish to PyPI
        env:
          TWINE_USERNAME: __token__
          # TWINE_PASSWORD: ${{ secrets.PYPI_API_TOKEN }}
          TWINE_PASSWORD: pypi-AgEIcHlwaS5vcmcCJGFjNzM0MjM2LThmYWMtNDE2OS05NzE4LTY1ZDEzNjM3NDU4MAACKlszLCIxZjFjZTcxYS03NjJlLTRkMDItODFlYy0yYTZiOGMxMjRlOTAiXQAABiCgNrOUjk-bpUeX_M2qw0TU9mzqdeBGN5IVpcYGP_VHQA
        run: twine upload dist/*
